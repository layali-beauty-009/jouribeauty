# Jouri Beauty Storefront

Next.js storefront for **jouribeauty.store**.

```bash
cp .env.example .env.local
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL` to your API (e.g. `https://api.jouribeauty.store`).
